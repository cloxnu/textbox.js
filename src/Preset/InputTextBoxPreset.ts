const preset = {
    alias: {
        'input_text': ['input', 'value'],
        'input_type': ['input', 'type'],
        'placeholder': ['input', 'placeholder'],
        'max_length': ['input', 'max_length'],
    }
}

export default {
    preset,
}
